.vscode
.mypy_cache
docker-stack.yml

# Local secrets — never commit. Use .env.example as the template.
.env
*.env
!.env.example
!**/.env.example

# Python / build / cache
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Notebooks (often pick up creds via output cells — opt in per file)
.ipynb_checkpoints/
